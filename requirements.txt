# Benötigte Python-Version: >= 3.11
# Enthält nur direkt importierte Pakete (keine transitiven Abhängigkeiten).
# Hinweis: torch ist als CUDA-12.1-Variante angegeben (Windows/GPU).
# Mac/Linux: install_mac_linux.sh installiert automatisch die CPU-Version.
# Installation: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

--extra-index-url https://download.pytorch.org/whl/cu121

contextily==1.7.0
geopandas==1.1.4
holidays==0.91
matplotlib==3.10.8
numpy==1.26.4
optuna==4.8.0
pandas==2.3.3
pyogrio==0.13.0
pyproj==3.7.2
scikit-learn==1.8.0
seaborn==0.13.2
shapely==2.1.2
torch==2.5.1+cu121
