# Automatisch generierte und manuell bereinigte requirements.txt
# Enthält nur die Haupt-Module, die der aktuelle Stand des Projektes (MATA-2026) direkt benötigt.
# Bitte neue Module nur hier hinzufügen, wenn sie aktiv im Projekt importiert werden.

holidays==0.91
matplotlib==3.10.8
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.8.0
seaborn==0.13.2
torch==2.11.0

# Optional (für Jupyter/Colab Notebooks)
ipykernel==7.2.0
